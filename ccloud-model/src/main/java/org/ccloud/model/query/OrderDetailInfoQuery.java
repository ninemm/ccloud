/**
 * Copyright (c) 2015-2016, Eric Huang 黄鑫 (hx50859042@gmail.com).
 *
 * Licensed under the GNU Lesser General Public License (LGPL) ,Version 3.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.gnu.org/licenses/lgpl-3.0.txt
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
package org.ccloud.model.query;

import java.util.LinkedList;
import java.util.List;

import org.ccloud.model.OrderDetailInfo;
import org.ccloud.utils.DateUtils;

import com.jfinal.kit.StrKit;
import com.jfinal.plugin.activerecord.Db;
import com.jfinal.plugin.activerecord.Page;
import com.jfinal.plugin.activerecord.Record;
import com.jfinal.plugin.ehcache.IDataLoader;

/**
 * Generated by 九毫米(http://9mm.tech).
 */
public class OrderDetailInfoQuery extends JBaseQuery { 

	protected static final OrderDetailInfo DAO = new OrderDetailInfo();
	private static final OrderDetailInfoQuery QUERY = new OrderDetailInfoQuery();

	public static OrderDetailInfoQuery me() {
		return QUERY;
	}

	public OrderDetailInfo findById(final String id) {
		return DAO.getCache(id, new IDataLoader() {
			@Override
			public Object load() {
				return DAO.findById(id);
			}
		});
	}

	public Page<OrderDetailInfo> paginate(int pageNumber, int pageSize, String orderby) {
		String select = "select * ";
		StringBuilder fromBuilder = new StringBuilder("from `cc_order_detail_info` ");

		LinkedList<Object> params = new LinkedList<Object>();

		if (params.isEmpty())
			return DAO.paginate(pageNumber, pageSize, select, fromBuilder.toString());

		return DAO.paginate(pageNumber, pageSize, select, fromBuilder.toString(), params.toArray());
	}

	public int batchDelete(String... ids) {
		if (ids != null && ids.length > 0) {
			int deleteCount = 0;
			for (int i = 0; i < ids.length; i++) {
				if (DAO.deleteById(ids[i])) {
					++deleteCount;
				}
			}
			return deleteCount;
		}
		return 0;
	}

	public List<Record> findByOrderId(String orderId) {
		StringBuilder fromBuilder = new StringBuilder("SELECT o.*, cc.`name`, cc.big_unit, cc.big_price,cc.`code` FROM cc_order_detail_info o ");
		fromBuilder.append("LEFT JOIN cc_product_info cc ON o.sell_product_id = cc.id ");
		fromBuilder.append("WHERE o.order_id = ? ");
		return Db.find(fromBuilder.toString(), orderId);
	}

	public List<Record> getOtherProductCount(String startDate, String endDate, String sellerId, String dayTag) {
		if (dayTag != null) {
			String[] date = DateUtils.getStartDateAndEndDateByType(dayTag);
			startDate = date[0];
			endDate = date[1];
		}
		StringBuilder fromBuilder = new StringBuilder("SELECT cp.`name` ,IFNULL(SUM(o.product_count),0) as productCount, IFNULL(SUM(o.product_amount),0) as totalAmount ,COUNT(*) as orderCount ");
		fromBuilder.append("FROM cc_order_detail_info o LEFT JOIN cc_product_info cp on cp.id = o.sell_product_id ");
		LinkedList<Object> params = new LinkedList<Object>();
		boolean needWhere = true;
		
		if (needWhere) {
			fromBuilder.append("WHERE 1 = 1 ");
		}
		
		if (StrKit.notBlank(startDate)) {
			fromBuilder.append(" and o.create_date >= ?");
			params.add(startDate);
		}

		if (StrKit.notBlank(endDate)) {
			fromBuilder.append(" and o.create_date <= ?");
			params.add(endDate);
		}
		fromBuilder.append("GROUP BY o.sell_product_id ");
		fromBuilder.append("ORDER BY totalAmount desc ");
		if (params.isEmpty())
			return Db.find(fromBuilder.toString());

		return Db.find(fromBuilder.toString(), params.toArray());
	}

	
}
