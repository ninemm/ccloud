/**
 * Copyright (c) 2015-2016, Eric Huang 黄鑫 (hx50859042@gmail.com).
 *
 * Licensed under the GNU Lesser General Public License (LGPL) ,Version 3.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.gnu.org/licenses/lgpl-3.0.txt
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
package org.ccloud.model.query;

import java.util.LinkedList;
import java.util.List;

import org.ccloud.model.PriceSystem;

import com.jfinal.plugin.activerecord.Db;
import com.jfinal.plugin.activerecord.Page;
import com.jfinal.plugin.activerecord.Record;

/**
 * Generated by 九毫米(http://9mm.tech).
 */
public class PriceSystemQuery extends JBaseQuery {

	protected static final PriceSystem DAO = new PriceSystem();
	private static final PriceSystemQuery QUERY = new PriceSystemQuery();

	public static PriceSystemQuery me() {
		return QUERY;
	}

	public PriceSystem findById(final String id) {
		return DAO.findById(id);
	}

	public Record findMoreById(final String id) {
		StringBuilder fromBuilder = new StringBuilder(" select  ");
		fromBuilder.append(" c.id, c.name, c.factor, c.dept_id, c.data_area, d.dept_name ");
		fromBuilder.append(" from `cc_price_system` c left join department d on c.dept_id = d.id ");
		fromBuilder.append(" where c.id = ? ");

		return Db.findFirst(fromBuilder.toString(), id);
	}

	public Page<Record> paginate(int pageNumber, int pageSize, String keyword, String dataArea) {
		String select = "select c.id, c.name, c.factor, d.dept_name ";
		StringBuilder fromBuilder = new StringBuilder(" from `cc_price_system` c ");
		fromBuilder.append(" left join department d on c.dept_id = d.id ");

		LinkedList<Object> params = new LinkedList<Object>();
		boolean needWhere = true;

		needWhere = appendIfNotEmptyWithLike(fromBuilder, "c.name", keyword, params, needWhere);
		needWhere = appendIfNotEmpty(fromBuilder, "c.data_area", dataArea, params, needWhere);

		fromBuilder.append(" order by c.create_date ");

		if (params.isEmpty())
			return Db.paginate(pageNumber, pageSize, select, fromBuilder.toString());

		return Db.paginate(pageNumber, pageSize, select, fromBuilder.toString(), params.toArray());
	}

	public List<Record> findPriceSystemByDeptId(String deptId, String dataArea) {
		StringBuilder sqlBuilder = new StringBuilder(" select c.id, c.name ");
		sqlBuilder.append(" from `cc_price_system` c ");

		LinkedList<Object> params = new LinkedList<Object>();
		boolean needWhere = true;

		needWhere = appendIfNotEmpty(sqlBuilder, "c.dept_id", deptId, params, needWhere);
		needWhere = appendIfNotEmpty(sqlBuilder, "c.data_area", dataArea, params, needWhere);

		sqlBuilder.append(" order by c.create_date ");

		return Db.find(sqlBuilder.toString(), params.toArray());
	}

	public int batchDelete(String... ids) {
		if (ids != null && ids.length > 0) {
			int deleteCount = 0;
			for (int i = 0; i < ids.length; i++) {
				if (DAO.deleteById(ids[i])) {
					++deleteCount;
				}
			}
			return deleteCount;
		}
		return 0;
	}

}
