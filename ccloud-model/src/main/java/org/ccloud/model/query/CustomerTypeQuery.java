/**
 * Copyright (c) 2015-2016, Eric Huang 黄鑫 (hx50859042@gmail.com).
 *
 * Licensed under the GNU Lesser General Public License (LGPL) ,Version 3.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.gnu.org/licenses/lgpl-3.0.txt
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
package org.ccloud.model.query;

import java.util.ArrayList;
import java.util.HashMap;
import java.util.LinkedList;
import java.util.List;
import java.util.Map;

import org.ccloud.model.CustomerType;
import org.ccloud.model.ModelSorter;

import com.google.common.collect.Lists;
import com.jfinal.kit.StrKit;
import com.jfinal.plugin.activerecord.Db;
import com.jfinal.plugin.activerecord.Page;
import com.jfinal.plugin.activerecord.Record;
import com.jfinal.plugin.ehcache.IDataLoader;

/**
 * Generated by 九毫米(http://9mm.tech).
 */
public class CustomerTypeQuery extends JBaseQuery {

	protected static final CustomerType DAO = new CustomerType();
	private static final CustomerTypeQuery QUERY = new CustomerTypeQuery();

	public static CustomerTypeQuery me() {
		return QUERY;
	}
	
	public Record findById(final String id) {
		StringBuilder fromBuilder = new StringBuilder(" select ");
		fromBuilder.append(" c.id, c.parent_id, cc.name as parent_name, c.name, c.code, c.price_system_id, c.proc_def_key, p.name as price_system_name");
		fromBuilder.append(" from `cc_customer_type` c left join `cc_price_system` p on c.price_system_id = p.id ");
		fromBuilder.append(" left join cc_customer_type cc on cc.id = c.parent_id");
		fromBuilder.append(" where c.id = ? ");

		return Db.findFirst(fromBuilder.toString(), id);
	}

	public Record findMoreById(final String id) {
		StringBuilder fromBuilder = new StringBuilder(" select ");
		fromBuilder.append(
				" c.id, c.parent_id, cc.name as parent_name, c.name, c.code, c.price_system_id, p.name as price_system_name, c.proc_def_key, c.dept_id, c.data_area, d.dept_name ");
		fromBuilder.append(" from `cc_customer_type` c left join `cc_price_system` p on c.price_system_id = p.id ");
		fromBuilder.append(" left join department d on c.dept_id = d.id ");
		fromBuilder.append(" left join cc_customer_type cc on cc.id = c.parent_id");
		fromBuilder.append(" where c.id = ? ");

		return Db.findFirst(fromBuilder.toString(), id);
	}

	public Page<Record> paginate(int pageNumber, int pageSize, String keyword, String show, 
			String dataArea) {
		String select = "select c.id, c.name, c.code, c.is_show, p.name as price_system_name, d.dept_name ";
		StringBuilder fromBuilder = new StringBuilder(
				"from `cc_customer_type` c left join `cc_price_system` p on c.price_system_id = p.id ");
		fromBuilder.append(" left join department d on c.dept_id = d.id ");

		LinkedList<Object> params = new LinkedList<Object>();
		boolean needWhere = true;

		needWhere = appendIfNotEmptyWithLike(fromBuilder, "c.name", keyword, params, needWhere);
		needWhere = appendIfNotEmpty(fromBuilder, "c.is_show", show, params, needWhere);
		needWhere = appendIfNotEmpty(fromBuilder, "c.data_area", dataArea, params, needWhere);
		if(needWhere && StrKit.isBlank(dataArea)) {
			fromBuilder.append(" where c.dept_id = '0' ");
		}
		if(!needWhere && StrKit.isBlank(dataArea)) {
			fromBuilder.append(" and c.dept_id = '0' ");
		}
		fromBuilder.append(" order by c.create_date ");

		if (params.isEmpty())
			return Db.paginate(pageNumber, pageSize, select, fromBuilder.toString());

		return Db.paginate(pageNumber, pageSize, select, fromBuilder.toString(), params.toArray());
	}

	public boolean enable(String id, int show) {
		CustomerType ccCustomerType = DAO.findById(id);
		ccCustomerType.set("is_show", show);

		return ccCustomerType.saveOrUpdate();
	}

	public List<Record> findCustomerTypeList(String dataArea) {
		LinkedList<Object> params = new LinkedList<Object>();

		StringBuilder sqlBuilder = new StringBuilder(" select c.id, c.name ");
		sqlBuilder.append(" from `cc_customer_type` c ");
		sqlBuilder.append(" where c.is_show = 1 ");
		sqlBuilder.append(" and c.data_area = ? ");
		params.add(dataArea);
//		appendIfNotEmptyWithLike(sqlBuilder, "c.data_area", dataArea, params, false);
		sqlBuilder.append(" order by c.create_date ");

		return Db.find(sqlBuilder.toString(), params.toArray());
	}
	
	public List<CustomerType> findByDataArea(String dataArea) {
		
		LinkedList<Object> params = new LinkedList<Object>();
		
		StringBuilder sql = new StringBuilder("select *");
		sql.append(" from `cc_customer_type`");
		sql.append(" where is_show = 1");
		
		appendIfNotEmpty(sql, "data_area", dataArea, params, false);
		
		sql.append(" order by create_date");
		
		return DAO.find(sql.toString(), params.toArray());
		
	}

	//去除直营商选项
	public List<CustomerType> findByDataArea1(String dataArea) {
		
		LinkedList<Object> params = new LinkedList<Object>();
		
		StringBuilder sql = new StringBuilder("select *");
		sql.append(" from `cc_customer_type` c");
		sql.append(" where c.is_show = 1");
		sql.append(" and c.`code`!='G' ");
		appendIfNotEmpty(sql, "c.data_area", dataArea, params, false);
		
		sql.append(" order by c.create_date");
		
		return DAO.find(sql.toString(), params.toArray());
		
	}
	
	public String findIdByName(String name, String dataArea) {
		LinkedList<Object> params = new LinkedList<Object>();
		boolean needWhere = true;

		StringBuilder sqlBuilder = new StringBuilder(" select c.id ");
		sqlBuilder.append(" from `cc_customer_type` c ");
		needWhere = appendIfNotEmpty(sqlBuilder, "c.name", name, params, needWhere);
		needWhere = appendIfNotEmpty(sqlBuilder, "c.data_area", dataArea, params, needWhere);

		return Db.queryStr(sqlBuilder.toString(), params.toArray());
	}
	
	public int batchDelete(String... ids) {
		if (ids != null && ids.length > 0) {
			int deleteCount = 0;
			for (int i = 0; i < ids.length; i++) {
				if (DAO.deleteById(ids[i])) {
					++deleteCount;
				}
			}
			return deleteCount;
		}
		return 0;
	}

	public List<CustomerType> findByDept(String id) {
		return DAO.doFind("dept_id = ?", id);
	}

	public List<Record> getCustomerTypes(String DataArea){
		StringBuilder fromBuilder = new StringBuilder("SELECT id,name FROM cc_customer_type WHERE `is_show`=1 and data_area = '"+DataArea+"'");
		List<Record> list = Db.find(fromBuilder.toString());
		return list;
	}
	
	public CustomerType findDataAreaAndName(String dataArea,String code){
		String sql = "select * from cc_customer_type where data_area = '"+dataArea+"' and code = ?";
		return DAO.findFirst(sql, code);
	}

	public List<Record> findByMember(String customerId, String sellerId) {
		LinkedList<Object> params = new LinkedList<Object>();

		StringBuilder sql = new StringBuilder( "SELECT cct.id AS id , cct. name AS name ");
		sql.append("FROM cc_seller_customer csc ");
		sql.append("LEFT JOIN cc_customer_join_customer_type ccjct ON csc.id = ccjct.seller_customer_id ");
		sql.append("LEFT JOIN cc_customer_type cct ON ccjct.customer_type_id = cct.id ");
		sql.append("WHERE csc.seller_id = ? AND csc.customer_id = ? ");

		params.add(sellerId);
		params.add(customerId);

		return Db.find(sql.toString(), params.toArray());
	}

	public CustomerType findBySellerCustomer(String sellerId, String customerId) {

		StringBuilder sql = new StringBuilder( "SELECT cct.* ");
		sql.append("FROM cc_seller_customer csc ");
		sql.append("LEFT JOIN cc_customer_join_customer_type ccjct ON csc.id = ccjct.seller_customer_id ");
		sql.append("LEFT JOIN cc_customer_type cct ON ccjct.customer_type_id = cct.id ");
		sql.append("WHERE csc.seller_id = ? ");
		sql.append("AND csc.customer_id = ? ");

		return DAO.findFirst(sql.toString(), sellerId, customerId);
	}

	public CustomerType findByName(String name, String dataArea) {
		LinkedList<Object> params = new LinkedList<Object>();
		boolean needWhere = true;

		StringBuilder sqlBuilder = new StringBuilder(" select c.* ");
		sqlBuilder.append(" from `cc_customer_type` c ");
		needWhere = appendIfNotEmpty(sqlBuilder, "c.name", name, params, needWhere);
		needWhere = appendIfNotEmpty(sqlBuilder, "c.data_area", dataArea, params, needWhere);

		return DAO.findFirst(sqlBuilder.toString(), params.toArray());
	}
	
	public List<CustomerType> _findByDataArea(String dataArea) {
		
		StringBuilder sql = new StringBuilder("select *");
		sql.append(" from `cc_customer_type`");
		sql.append(" where is_show = 1");
		sql.append(" and data_area like '"+dataArea+"' ");
		sql.append(" order by create_date");
		
		return DAO.find(sql.toString());
		
	}
	
	public List<CustomerType> findByID(String customerTypeIds){
		String sql = "select * from cc_customer_type where id in ("+customerTypeIds+") ORDER BY create_date ";
		return DAO.find(sql);
	}

	public List<Map<String, Object>> findCustomerTypeListAsTree(int i, String dataArea) {
		List<CustomerType> list = findCustomerTypeList(null, dataArea, null);
		ModelSorter.tree(list);
		List<Map<String, Object>> resTreeList = new ArrayList<>();
		Map<String, Object> map = new HashMap<>();
		map.put("text", "客户类型");
		map.put("tags", Lists.newArrayList(0));
		map.put("nodes", doBuild(list));
		resTreeList.add(map);
		return resTreeList;
	}

	private List<CustomerType> findCustomerTypeList(String parentId, String dataArea, String orderBy) {
		final StringBuilder sqlBuilder = new StringBuilder("SELECT * FROM cc_customer_type c ");

		final List<Object> params = new LinkedList<Object>();
		boolean needWhere = true;

		needWhere = appendIfNotEmpty(sqlBuilder, "parent_id", parentId, params, needWhere);
		needWhere = appendIfNotEmpty(sqlBuilder, "c.data_area", dataArea, params, needWhere);

		buildOrderBy(orderBy, sqlBuilder);

		String key = buildKey(dataArea, null, null, null, orderBy);

		List<CustomerType> data = DAO.getFromListCache(key, new IDataLoader() {

			@Override
			public Object load() {
				if (params.isEmpty()) {
					return DAO.find(sqlBuilder.toString());
				}
				return DAO.find(sqlBuilder.toString(), params.toArray());
			}
		});

		if (data == null)
			return null;
		return new ArrayList<CustomerType>(data);
	}

	private String buildKey(String module, Object... params) {
		StringBuffer keyBuffer = new StringBuffer(module == null ? "" : "module:" + module);
		if (params != null && params.length > 0) {
			for (int i = 0; i < params.length; i++) {
				keyBuffer.append("-p").append(i).append(":").append(params[i]);
			}
		}
		return keyBuffer.toString().replace(" ", "");
	}

	private List<Map<String, Object>> doBuild(List<CustomerType> list) {
		List<Map<String, Object>> resTreeList = new ArrayList<>();
		for (CustomerType customerType : list) {
			Map<String, Object> map = new HashMap<>();
			map.put("text", customerType.getName());
			map.put("tags", Lists.newArrayList(customerType.getId()));
			resTreeList.add(map);

			if (customerType.getChildList() != null && customerType.getChildList().size() > 0) {
				map.put("nodes", doBuild(customerType.getChildList()));
			}
		}
		return resTreeList;
	}

	protected void buildOrderBy(String orderBy, StringBuilder fromBuilder) {

		fromBuilder.append(" order by ");
		if (StrKit.isBlank(orderBy)) {
			fromBuilder.append("c.parent_id,c.create_date asc ");
			return;
		}

		String orderbyInfo[] = orderBy.trim().split("\\s+");
		orderBy = orderbyInfo[0];

		fromBuilder.append("c.parent_id,c.create_date ");

		if (orderbyInfo.length == 1) {
			fromBuilder.append("desc");
		} else {
			fromBuilder.append(orderbyInfo[1]);
		}
	}

	public void updateParent(CustomerType customerType) {
		if (customerType != null) {
			CustomerType parent = this.findCustomerTypeById(customerType.getParentId());
			Integer childNum = this.childNumById(customerType.getParentId());
			if (parent != null && childNum > 0) {
				if (parent.getIsParent() == 0) {
					parent.setIsParent(1);
					parent.update();
				}
			} else {
				if (parent.getIsParent() > 0) {
					parent.setIsParent(0);
					parent.update();
				}
			}
		}
	}
	
	private CustomerType findCustomerTypeById(String parentId) {
		return DAO.doFindFirst("id = ?", parentId);
	}

	private Integer childNumById(String parentId) {
		Integer num = DAO.doFindCount("parent_id = ?", parentId).intValue();
		return num;
	}
	
}
